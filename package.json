{
  "name": "s3-cloudfront-video-app",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c green,blue \"npm:dev --workspace client\" \"npm:dev --workspace server\"",
    "build": "npm run build --workspace client && npm run build --workspace server",
    "start": "npm run start --workspace server && npm run preview --workspace client",
    "test": "npm run test:run --workspace server && npm run test:run --workspace client",
    "test:watch": "npm run test --workspace server && npm run test --workspace client",
    "test:server": "npm run test:run --workspace server",
    "test:client": "npm run test:run --workspace client",
    "test:coverage": "npm run test:coverage --workspace server && npm run test:coverage --workspace client",
    "test:ui": "concurrently -n server,client -c blue,green \"npm:test:ui --workspace server\" \"npm:test:ui --workspace client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
